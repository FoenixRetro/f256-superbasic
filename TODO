Progress:
	- map the sprite area into the mappable page to read the sprite address.
	- change to read using paging
	- move the sprite up into paged RAM.
	
	Graphics code.
		Plot points.
		Horizontal repeated characters
Later:
	Disable interrupts enable at end of line command ?
	Indenting

