Progress:

	Integer & Fractional functions
		NOT INT() ISVAL() VAL() DEC() FRAC() ALLOC()

	String allocation/soft code.
		String concatenation
		String unaries.
			CHR$() HEX$() LEFT$() MID$() RIGHT$() SPC() STR$()

	PRINT import.			

	Reading variable values / Assignment. Concreting strings. Generalise assignment

	Parenthesis checker.


Later:
	Expand rnd() (check MS Basic)
	Error : out of identifier space
	Todo : scan for procedures (part of CLEAR)	
	Code to access variables in term.
	Variable code
	Improve CALL	
	Reset procedures to arrays ??
