Progress:

	Integer compare is wrong for the same reasons.
	Test Integer Compare.
	Wire in unary functions and add testing for true/false and general unary functions

	Shift tokens
	Comparison strings.
	Clear code (also reset procedures to arrays ? - scan later)
	Float add, subtract, multiply, divide, compare (?) [short divide is wrong !]
	Float testing code
	Integer & Fractional functions
	
Later:
	Error : out of identifier space
	Todo : scan for procedures (part of CLEAR)	
	Code to access variables in term.
	Encoding Decimals (e.g. .90132)
	Variable code
	Improve CALL	