Progress:
	Float checks (use /1 initially)
	Add return B if zero check if works.
	Encoding Decimals (e.g. .90132), fix up float checks ?
	Float testing code
	Optimise normalisation & exponent shift in add.
	Integer & Fractional functions
	
Later:
	Error : out of identifier space
	Todo : scan for procedures (part of CLEAR)	
	Code to access variables in term.
	Variable code
	Improve CALL	
	Reset procedures to arrays ??

WATCH OUT FOR -0 when comparing values !
